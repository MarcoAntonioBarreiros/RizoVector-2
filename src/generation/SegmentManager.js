import{CONFIG}from'../data/config.js';export class SegmentManager{constructor(generator){this.generator=generator;this.segments=[]}reset(){this.segments=[];for(let i=0;i<CONFIG.world.segmentsAhead;i++)this.segments.push(this.generator.generate(-i*CONFIG.world.segmentHeight,CONFIG.world.segmentHeight))}update(dt,speed,viewportHeight){for(const s of this.segments)s.y+=speed*dt;this.segments=this.segments.filter(s=>s.y<viewportHeight+CONFIG.world.segmentHeight);const created=[];while(this.segments.length<CONFIG.world.segmentsAhead){const top=Math.min(...this.segments.map(s=>s.y));const s=this.generator.generate(top-CONFIG.world.segmentHeight,CONFIG.world.segmentHeight);this.segments.push(s);created.push(s)}return created}currentBiome(playerY){return this.segments.find(s=>playerY>=s.y&&playerY<s.y+s.height)?.biome||this.segments[0]?.biome||'organic'}}